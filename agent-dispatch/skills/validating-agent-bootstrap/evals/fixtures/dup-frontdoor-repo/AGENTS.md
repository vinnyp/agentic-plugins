# Pricer CLI — Agent Guide

Pricer is a **private, local-only** pricing CLI (Go).

**Local-only: no git remote, no PRs.** Commit directly to `main`; review happens locally.

Run `make test` before committing. Amounts are stored as integer cents, never floats.
