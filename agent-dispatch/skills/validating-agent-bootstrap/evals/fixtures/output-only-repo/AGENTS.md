# Dist — Agent Guide

Dist is the **publishing repo** for shipped artifacts (built binaries, packaged skills).

**Output only — don't run agent sessions from this repo.** Do all work in the dev repo
(`/path/to/widget-repo`) or the planning vault; finished artifacts land *here* as shipped output.
Reference secret *names*, never *values*.
