# Vault Tools — Agent Guide

Vault Tools is a private hub for local scripts.

This repo is for personal use — everything here **stays on this machine** and **nothing is published anywhere**. Commit to `main`.

## Build

Run `make build`.
